#include<iostream>
#include<string>
#include<typeinfo>
using namespace std;

int main(){
    int test=5;
    string n="harrykardam";
    string space=" ";
    string number="1";
    string s="kardam";
    // cout<<"Length of string :"<<s.length()<<endl; // length of the string.
    // cout<<"Size of string :"<<s.size()<<endl; // size of the string.
    // cout<<n.insert(5,space)<<endl; // to insert a string into a another string.
    // cout<<n.append(s)<<endl; // to add a two string.
    // cout<<s.back()<<endl; // to print last letter of string.
    // cout<<s.front()<<endl; // to print first letter of string.
    // cout<<s.at(2); // to print which value stored in particular indexing.
    // int a=stoi(number); // to convert string to integer(s to i).
    // cout<<typeid(a).name(); // to print datatype in code (int = i,float = f, string = NSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEE).
    // string convert = to_string(test); // convert into string.
    // n.push_back('l'); // for push a character in string.
    // cout<<n<<endl;
    // n.pop_back();
    // cout<<n<<endl; // for pop a last character in string.
    
    
    // cout<<a+1<<endl;
    // cout<<convert<<endl;
    // cout<<typeid(convert).name();


    return 0;
}